import styled from 'styled-components';

export const ListItem = styled.li`
  margin: 5px 0;
  padding: 5px;

  text-align: center;

  &:hover {
    cursor: pointer;
    background-color: #eee;
  }
`;
